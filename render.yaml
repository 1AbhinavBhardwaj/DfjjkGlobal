services:
  # Spring Boot Backend Web Service
  - type: web
    name: dfjjkglobal-backend
    env: docker
    dockerContext: ./backend
    dockerfilePath: ./backend/Dockerfile
    region: oregon
    plan: free

databases:
  # Free PostgreSQL Database Service on Render
  - name: dfjjkglobal-db
    region: oregon
    plan: free
